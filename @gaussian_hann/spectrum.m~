function spectrum(p)

N = 1024;
f = zeros(2*N,1);
w = zeros(2*N,1);
for i = 1:N
  t = (i-1)/(N-1)*p.duration;
  w(i) = omega1(p,t(i));
end

for i = (-N+1):N 
  f(i) = ((i-1)/(N-1))*p.duration;
  


plot(t,real(w),t,imag(w));
xlabel('time')